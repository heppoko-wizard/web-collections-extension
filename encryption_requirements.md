# Encryption Requirements (暗号化要件)

このドキュメントは、Web Collections 拡張機能におけるアイテムの暗号化機能に関する仕様を定義したものです。

## 概要
ユーザーが任意の復号キー（パスワード。1文字以上）を設定できる簡便な暗号化機能を提供します。
これにより、ブックマーク上に保存されるアイテム（URLやタイトル）が難読化され、拡張機能内からのみ正しいパスワードを用いて復元・表示できるようになります。

## 技術仕様
- ブラウザ標準の **Web Crypto API (AES-GCM)** を使用します。
- パスワードからキーを導出するため、**PBKDF2** (SHA-256, 100,000 iterations) を使用します。

## 保存時の挙動
1. 本来のタイトル、URL、画像URL、メモなどのデータをJSONオブジェクトにまとめます。
2. 上記のJSONを暗号化し、IV（Initial Vector）とSaltを含めたバイト配列をBase64文字列に変換します。
3. ブックマークのタイトルは、暗号化されていることが識別できる固定文字列（例: `🔒 Encrypted Item`）とします。
4. ブックマークのURLは、誤クリック時に開けないようダミーのスキーム/URL（例: `https://web-collections.local/?enc=<Base64暗号化データ>`）として保存します。

## 読み込み時の挙動
1. 拡張機能側で事前に設定されたキー（パスワード）自動で読み込みます(毎回の入力はいりません)。
2. ブックマークのURLがダミーURLのプレフィックス（`https://web-collections.local/?enc=`）で始まっている場合、暗号化アイテムと判定します。
3. URLからBase64文字列を抽出し、パスワードを用いて復号します。
4. 復号されたJSONデータを用いて、本来のタイトル、URL、サムネイル画像等をUI上に復元・描画します。
5. パスワードが未設定、または誤っている場合は、UI上で「復号失敗（🔒 Decryption Failed）」等として扱い、中身へのアクセスを防ぎます。

## 制約事項
- パスワードの変更は、これからの保存に対してのみ適用されます。すでに暗号化されて保存された既存のアイテムを自動的に再暗号化（マイグレーション）する処理は、現在の仕様では含みません。
-暗号化を行う際は、以降の登録はもちろんのこと、過去のブックマークに登録されている全ての登録済みコレクションも暗号化書き換えを行う。(警告を出すこと)
